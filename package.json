{
  "scripts": {
    "dev": "npm run build -- --sourcemap=inline --watch",
    "build": "esbuild js/widget.jsx --minify --format=esm --bundle --outdir=src/widget_bandsplot/static"
  },
  "dependencies": {
    "@anywidget/react": "^0.0.4",
    "chart.js": "^4.4.2",
    "chartjs-plugin-annotation": "^3.0.1",
    "chartjs-plugin-zoom": "^2.0.1",
    "mc-react-bands": "^0.5.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
